--- Phase: ra ---
MachineFunction 'stores_not_merged' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/4 ; %6
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3
  $rcx = MOP_COPY.4 $rsi ; %6

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  mov.4 [$rax], $rcx ; %10
  mov.4 [$rax], $rcx ; %13

MBB #1, '<exit>', ir #1, <- #2
  ret ; %15


