--- Phase: ra ---
MachineFunction 'small' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/8 ; %6
  %v2 : gp/8
  %v3 : gp/8 ; %21
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3
  $rcx = MOP_COPY.8 $rsi ; %6

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rdx = mov.8 [$rcx] ; %12
  mov.8 [$rax], $rdx ; %12
  $rdx = mov.8 [$rcx + 8] ; %12
  mov.8 [$rax + 8], $rdx ; %12
  $rdx = mov.8 [$rcx + 16] ; %12
  mov.8 [$rax + 16], $rdx ; %12
  $rcx = mov.8 [$rax] ; %21

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.8 $rcx ; %24
  ret ; %24


--- Phase: ra ---
MachineFunction 'large' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/8 ; %6
  %v2 : gp/8 ; %21
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3
  $rdx = MOP_COPY.8 $rsi ; %6

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rcx = mov.8 512 ; %12
  $rdi = lea.8 [$rax] ; %12
  $rsi = lea.8 [$rdx] ; %12
  rep movsb.8 [implicit-def $rdi, implicit-def $rsi, implicit-def $rcx, implicit $rdi, implicit $rsi, implicit $rcx] ; %12
  $rcx = mov.8 [$rax] ; %21

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.8 $rcx ; %24
  ret ; %24


