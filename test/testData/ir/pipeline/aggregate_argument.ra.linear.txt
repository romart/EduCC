--- Phase: ra ---
MachineFunction 'pass' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %0
  %v1 : gp/8 ; %8
  %v2 : gp/8
  %v3 : gp/8
  %v4 : gp/4 ; %6
Frame: 32 bytes, 32 outgoing
  fi#0 : param 24/8 @ 16 's' ; %2
  fi#1 : local 24/8 @ -24 ; %8
Allocator: linear scan
Physical registers used: $rax $rcx $rsp
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rsp ; %0

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rax = lea.8 [fi#1] ; %8
  $rax = mov.8 [fi#0] ; %9
  mov.8 [fi#1], $rax ; %9
  $rax = mov.8 [fi#0 + 8] ; %9
  mov.8 [fi#1 + 8], $rax ; %9
  $rax = mov.8 [fi#0 + 16] ; %9
  mov.8 [fi#1 + 16], $rax ; %9
  $rax = mov.8 [fi#1] ; %6
  mov.8 [$rsp], $rax ; %6
  $rax = mov.8 [fi#1 + 8] ; %6
  mov.8 [$rsp + 8], $rax ; %6
  $rax = mov.8 [fi#1 + 16] ; %6
  mov.8 [$rsp + 16], $rax ; %6
  call takeBig [implicit-def $rax] <clobbers caller-saved> ; %6
  $rcx = MOP_COPY.4 $rax ; %6

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.4 $rcx ; %12
  ret ; %12


--- Phase: ra ---
MachineFunction 'scalars' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %0
  %v1 : gp/4 ; %3
  %v2 : gp/4 ; %8
  %v3 : gp/8 ; %15
  %v4 : gp/8
  %v5 : gp/8
  %v6 : gp/4 ; %12
Frame: 32 bytes, 32 outgoing
  fi#0 : param 24/8 @ 16 's' ; %6
  fi#1 : local 24/8 @ -24 ; %15
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsp $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rsp ; %0
  $rax = MOP_COPY.4 $rdi ; %3
  $rcx = MOP_COPY.4 $rsi ; %8

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rdx = lea.8 [fi#1] ; %15
  $rdx = mov.8 [fi#0] ; %16
  mov.8 [fi#1], $rdx ; %16
  $rdx = mov.8 [fi#0 + 8] ; %16
  mov.8 [fi#1 + 8], $rdx ; %16
  $rdx = mov.8 [fi#0 + 16] ; %16
  mov.8 [fi#1 + 16], $rdx ; %16
  $rdx = mov.8 [fi#1] ; %12
  mov.8 [$rsp], $rdx ; %12
  $rdx = mov.8 [fi#1 + 8] ; %12
  mov.8 [$rsp + 8], $rdx ; %12
  $rdx = mov.8 [fi#1 + 16] ; %12
  mov.8 [$rsp + 16], $rdx ; %12
  $rdi = MOP_COPY.4 $rax ; %12
  $rsi = MOP_COPY.4 $rcx ; %12
  call mixed [implicit-def $rax, implicit $rdi, implicit $rsi] <clobbers caller-saved> ; %12
  $rcx = MOP_COPY.4 $rax ; %12

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.4 $rcx ; %20
  ret ; %20


--- Phase: ra ---
MachineFunction 'small' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/8 ; %10
  %v2 : gp/4 ; %7
Frame: 16 bytes
  fi#0 : local 8/4 @ -8 's' ; %2
Allocator: linear scan
Physical registers used: $rax $rcx $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3
  mov.8 [fi#0], $rax ; %4

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rax = mov.8 [fi#0] ; %10
  $rdi = MOP_COPY.8 $rax ; %7
  call takeSmall [implicit-def $rax, implicit $rdi] <clobbers caller-saved> ; %7
  $rcx = MOP_COPY.4 $rax ; %7

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.4 $rcx ; %13
  ret ; %13


