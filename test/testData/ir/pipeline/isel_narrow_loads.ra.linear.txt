--- Phase: ra ---
MachineFunction 'promoted' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/4 ; %8
  %v2 : gp/4 ; %9
  %v3 : gp/4 ; %13
  %v4 : gp/1 ; %14
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rcx = movsx.4/1 [$rax] ; %8
  $rax = MOP_COPY.4 $rcx ; %9
  $rcx = MOP_COPY.4 $rax ; %13
  $rcx = add.4 $rcx, 1 ; %13
  $rdx = MOP_COPY.1 $rcx ; %14

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.1 $rdx ; %17
  ret ; %17


--- Phase: ra ---
MachineFunction 'unpromoted' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/4 ; %7
  %v2 : gp/4 ; %8
  %v3 : gp/4 ; %10
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rcx = movzx.4/1 [$rax] ; %7
  $rax = MOP_COPY.4 $rcx ; %8
  $rcx = MOP_COPY.4 $rax ; %10
  $rcx = add.4 $rcx, 1 ; %10

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.4 $rcx ; %13
  ret ; %13


--- Phase: ra ---
MachineFunction 'halfword' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/4 ; %7
  %v2 : gp/4 ; %8
  %v3 : gp/4 ; %10
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rcx = movsx.4/2 [$rax] ; %7
  $rax = MOP_COPY.4 $rcx ; %8
  $rcx = MOP_COPY.4 $rax ; %10
  $rcx = add.4 $rcx, 1 ; %10

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.4 $rcx ; %13
  ret ; %13


--- Phase: ra ---
MachineFunction 'mixedSource' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/8 ; %6
  %v2 : gp/1 ; %9
  %v3 : gp/4 ; %12
  %v4 : gp/4 ; %13
  %v5 : gp/4 ; %15
  %v6 : gp/1 ; %20
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3
  $rcx = MOP_COPY.8 $rsi ; %6
  $rsi = MOP_COPY.1 $rdx ; %9

MBB #2, '<FIRST>', ir #2, <- #0, -> #4 #5
  $rdx = movsx.4/1 [$rax] ; %12
  $rdi = MOP_COPY.4 $rdx ; %13
  $rdx = movsx.4/1 $rsi ; %15
  cmp.4 $rdi, $rdx ; %16
  jl #4 ; %17

MBB #5, '<crit_splitter>', ir #5, <- #2, -> #3
  jmp #3 ; %29

MBB #4, '<if_then>', ir #4, <- #2, -> #3
  $rdx = movsx.4/1 [$rax] ; %20
  mov.1 [$rcx], $rdx ; %21

MBB #3, '<if_exit>', ir #3, <- #5 #4, -> #1

MBB #1, '<exit>', ir #1, <- #3
  ret ; %24


--- Phase: ra ---
MachineFunction 'toWide' [target = x86_64]
VRegs:
  %v0 : gp/8 ; %3
  %v1 : gp/8 ; %6
  %v2 : gp/1 ; %11
  %v3 : gp/8 ; %12
  %v4 : gp/4 ; %16
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = MOP_COPY.8 $rdi ; %3
  $rcx = MOP_COPY.8 $rsi ; %6

MBB #2, '<FIRST>', ir #2, <- #0, -> #1
  $rdx = movsx.4/1 [$rcx] ; %11
  $rcx = movsx.8/1 $rdx ; %12
  $rdx = mov.4 [$rax + $rcx*4] ; %16

MBB #1, '<exit>', ir #1, <- #2
  $rax = MOP_COPY.4 $rdx ; %19
  ret ; %19


--- Phase: ra ---
MachineFunction 'byteLoop' [target = x86_64]
VRegs:
  %v0 : gp/4 ; %42
  %v1 : gp/4 ; %11
  %v2 : gp/4 ; %40
  %v3 : gp/4 ; %26
  %v4 : gp/4 ; %31
  %v5 : gp/8 ; %3
  %v6 : gp/4 ; %6
  %v7 : gp/8 ; %21
  %v8 : gp/4 ; %23
  %v9 : gp/4 ; %24
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsi $rdi $r8
MBB #0, '<entry>', ir #0, -> #2
  $rax = mov.4 0 ; %11
  $rcx = MOP_COPY.8 $rdi ; %3
  $rdx = MOP_COPY.4 $rsi ; %6

MBB #2, '<FIRST>', ir #2, <- #0, -> #3
  $rsi = MOP_COPY.4 $rax ; %42
  $rdi = MOP_COPY.4 $rax ; %40

MBB #3, '<for_head>', ir #3, <- #2 #6, -> #4 #5
  cmp.4 $rdi, $rdx ; %17
  jge #5 ; %18

MBB #4, '<for_body>', ir #4, <- #3, -> #6
  $rax = movsx.8/4 $rdi ; %21
  $r8 = movzx.4/1 [$rcx + $rax*1] ; %23
  $rax = MOP_COPY.4 $r8 ; %24
  $r8 = MOP_COPY.4 $rsi ; %26
  $r8 = add.4 $r8, $rax ; %26

MBB #6, '<for_mod>', ir #6, <- #4, -> #3
  $rax = MOP_COPY.4 $rdi ; %31
  $rax = add.4 $rax, 1 ; %31
  $rsi = MOP_COPY.4 $r8 ; %42
  $rdi = MOP_COPY.4 $rax ; %40
  jmp #3 ; %33

MBB #5, '<for_exit>', ir #5, <- #3, -> #1

MBB #1, '<exit>', ir #1, <- #5
  $rax = MOP_COPY.4 $rsi ; %37
  ret ; %37


--- Phase: ra ---
MachineFunction 'throughPhi' [target = x86_64]
VRegs:
  %v0 : gp/4 ; %28
  %v1 : gp/4 ; %10
  %v2 : gp/4 ; %17
  %v3 : gp/8 ; %3
  %v4 : gp/4 ; %6
  %v5 : gp/4 ; %16
  %v6 : gp/4 ; %22
Frame: <empty>
Allocator: linear scan
Physical registers used: $rax $rcx $rdx $rsi $rdi
MBB #0, '<entry>', ir #0, -> #2
  $rax = mov.4 0 ; %10
  $rcx = MOP_COPY.8 $rdi ; %3
  $rdx = MOP_COPY.4 $rsi ; %6

MBB #2, '<FIRST>', ir #2, <- #0, -> #4 #5
  cmp.4 $rdx, $rax ; %13
  jg #4 ; %14

MBB #5, '<crit_splitter>', ir #5, <- #2, -> #3
  $rdx = MOP_COPY.4 $rax ; %28
  jmp #3 ; %34

MBB #4, '<if_then>', ir #4, <- #2, -> #3
  $rax = movsx.4/1 [$rcx] ; %16
  $rcx = MOP_COPY.4 $rax ; %17
  $rdx = MOP_COPY.4 $rcx ; %28

MBB #3, '<if_exit>', ir #3, <- #5 #4, -> #1
  $rcx = MOP_COPY.4 $rdx ; %22
  $rcx = add.4 $rcx, 1 ; %22

MBB #1, '<exit>', ir #1, <- #3
  $rax = MOP_COPY.4 $rcx ; %25
  ret ; %25


