

typedef int t_i, *pt_i, **ppt_i;

int foo(int x, t_i xx, t_i **y, pt_i pz, pt_i *ppz, ppt_i a, ppt_i *pa, ppt_i **ppa) {
  yyin = fopen("test.txt", "r");
  yylex();
//  printf("%d %d %d %d\n", x, x * x, num_lines, num_chars);
  return 0;
}

int (*(qux(int (******f)(double (*****)(struct S*))))) {}

int *fxx(int p);

int *(***(x)[20][30][40])(int *(*)(int, int **(**)(int, int**)), int);

int *(*xx[10])(int);

typedef int (*f_t)(int, int);

enum E1 {
  EC_1 = -1,
  EC_2 = EC_1 + 15,
  EC_3
};

f_t fax(int a, int b) {
  int c = EC_3;
  for (c = a; c < b; ++c) {
    printf("%d %d %d", a, b, c);
  }
}

static x;
int foo1(void) {
  int xx = 42;
  long double xxx = 10;
  unsigned char xxxx = xx + xxx;
}

int foo2(void) {
  struct SL { int a; } a = { 0 }, b = { 0 }, *c = NULL, d;
}

int f() {
    aa: bar();

    bb = cc + dd ;
    if (ee) {
        printf("1");
    } else printf("2");
}

struct S;

struct SS {
    int a;
    int b, d;
    typedef struct {
        double x;
        struct XXX ****xx;
    } ss1, *ss2, **ss3;

    ss1 v1, *pv1, **pv1;
    ss2 v2, *pv2, **pv2;
    ss3 v3, *pv3, **pv3;
};

struct {

};
